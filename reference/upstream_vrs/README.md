# Upstream architecture reference

These source snapshots preserve pre-VRS2 architecture definitions for source
lineage review. They are not a Python package, runtime fallback, migration path,
or test dependency. The live implementation is `src/swegca_vrs2`, and its
ported first-party definitions are sealed by `NATIVE_VRS2_PORT.json`.
