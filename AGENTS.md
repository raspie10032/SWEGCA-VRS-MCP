# 새 SWEGCA VRS C++ 구현

- 이 저장소의 새 구현은 이전 C++ VRS 코드, 빌드 파일, 후보 설계 문서를 복사하거나 참조하지 않는다.
- 기존에 완성한 `cpp/swegca_architecture/` SWEGCA 코어는 그대로 유지한다. 처음부터 다시 작성할 대상은 그 코어를 논리소자로 조합하는 VRS다. 코어 재작성은 작업 범위가 아니다.
- SWEGCA 코어를 논리소자로 삼아 VRS와 기억 4단계의 기능을 구성한다. 모든 절차의 결정은 SWEGCA 코어를 통해 이루어진다.
- VRS는 블록의 경험을 섞고 SWEGCA 코어로 검증한 결과로 연결 강도를 갱신한다. 승인하면 강화하고, 반려하면 약화하고, 기권하면 유지한다. 경험의 신뢰도는 연결 강도에서 읽는다.
- 사용자 입력은 즉시 Déjà vu의 키가 된다. 이후 Recall은 원경험 주소를 찾고, Replay는 선택한 원경험을 읽으며, Re-evidence는 현재 명제와 충돌할 때 실행한다.
- 로제핀 Main이 정체성, 누적 경험, VRS, 판단과 권한을 소유한다. 출처, 불확실성, 반증을 보존한다. 실시간 내부 LLM 호출은 0회다.
- 새 코드는 사용자의 지시와 원본 SWEGCA 아키텍처를 근거로 작성한다. 삭제된 C++ 후보를 구현 근거로 사용하지 않는다.
