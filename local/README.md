# Evaluation assets

`local/bench` contains the VRS scale and context compaction evaluations. Runtime
conversation capture is implemented by `swegca_vrs2.conversation_*` and the
packaged hook configuration generator `swegca-vrs2-codex-hooks`.

No transcript log adapter, compatibility importer, or external agent outbox is
part of the package or evaluation path.
