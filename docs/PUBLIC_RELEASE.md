# Public source release — 2026-09-07

## Scope and current-turn preflight

The user requested a separate public GitHub repository to try from Claude Code.
Publish the verified read-only preview plus connection instructions, keeping the
unfinished stateful integration intact locally and explicitly absent from this
release. This does not fulfill or cancel the earlier stateful-core request.
No private history, experience, keys, models or settings are publication inputs.
No private service restart, host registration, new goal or growth experiment.

1. Identity ownership: yes. No private entity is changed; the preview server
   exclusively holds its own immutable operator-supplied diagnostic index.
2. Specialist role: yes. No embedded LLM or 135.5M/E2B substitution; caller
   assessments remain untrusted proposals, not authority.
3. Experience access: yes. All supplied records remain hot-addressable; no
   evaluator allowlist. This does not claim access to unprovided private data.
4. Freeze boundary: yes. One dataset generation per server launch; this preview
   supports replacement at the next launch, not a permanent experience cap.
5. New experience: yes. Synthetic rows and repeated software tests are explicitly
   not new observed experience or evidence of learning.
6. Evaluation: yes. Tests establish software/transport behavior only; growth
   would require longitudinal new outcomes, retention and refutation evidence.
7. Model replacement: yes. The server has no model dependency; external agents
   consume the same MCP without becoming the owner of its source provenance.
8. Authority: yes. No write/action/model-update/P3 tool or held-out material.
   Public GitHub creation is explicitly authorized by the current user request;
   Claude Code host configuration remains for the user to perform.

AGENTS preflight: PASS (bounded public-source publication, not a core phase pass).

## Release verification

Fresh checks completed against a clean Git-index export, not the dirty development
directory. The latter contains untracked, unfinished stateful modules and
architecture imports, all preserved locally and excluded from this release.

- Locked dependency installation succeeded: Python 3.12.14, MCP 2.1.1.
- Full published suite: **91 passed in 1.95 seconds**.
- Port integrity manifest: **8 files PASS**; no numerical algorithm edits.
- Wheel and sdist built; inventories contain the public preview only. Source
  archive includes the synthetic fixture, provenance manifest and verification
  tools. No stateful, architecture, key configuration or private data files.
- Wheel installed into another environment; real subprocess MCP smoke passed
  in both `auto` and `legacy` modes and closed each child afterwards.
- Both runs discovered the four documented tools; the synthetic five-arm result
  was `success / abstain / abstain / abstain / success`. All authority flags false.
- Published paths were checked for common credential patterns and private local
  absolute paths: no matches. This is a scoped inspection, not a guarantee that
  a pattern scanner detects every possible secret. The only pre-release history
  is the standalone initial commit, not private project history.
- Public source license remains MIT; unfinished MPL architecture imports were
  not staged, packaged or relicensed. No registry package is being published.
- Claude Code executable was not available on this host's PATH. Its documented
  registration syntax was checked against the official guide; a live Claude Code
  tool call is left unverified and is not inferred from the SDK tests.

The release-note completion edit changes documentation only after these checks.
Remote creation, push, anonymous source access and remote/local SHA equality are
the final publication checks; their result is reported in the user-facing handoff.

## Rozephine의 판단

Not invoked; no private cognition or experience is used in this release.

## Codex의 판단

Claude Code is the client and SWEGCA+VRS is the called MCP server. The preview
exposes four read-only tools. Online accumulation, numerical graph convergence
and durable World commits remain unfinished, not silently counted as shipped.

## Codex 작업 실수 및 교정

Earlier implementation narrowed the full-core request to read-only diagnostics;
the unfinished repair remains local and this publication does not erase that
omission. The preceding architecture explanation also risked implying that the
server controlled the agent; README now states the actual client-to-server
direction. No reversed agent invocation exists in the published entry point.
During this publication, a combined contract read was truncated in the tool
output; the overlapping remainder was read separately before this preflight.
No release mutation was authorized using the truncated read alone.
